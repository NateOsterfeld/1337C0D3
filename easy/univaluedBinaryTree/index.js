function TreeNode(val) {
	this.val = val
	this.right = this.left = null
}

// Return true if the given tree is univalued, meaning every node in the tree has the same value
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var isUnivalTree = function(root) {
}
